@page "/tests"

@* View-everything catalog of the tests the runner can execute, grouped by
   Site → Env → Module. Read-only for now (no selection wiring). *@

<h1>Tests</h1>
<p class="muted">Everything the runner can execute, grouped by site.</p>

<TestBrowser />
